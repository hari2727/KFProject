export const maxLambdaAwaitTimeout = 20 * 1000;

export enum ContentTypes {
    XLSX = 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
    JSON = 'application/json',
}
export enum Locale {
    GB = 'GB',
    EN_UK = 'en-UK',
}
